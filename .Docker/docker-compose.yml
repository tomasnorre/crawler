version: '3.1'

services:
  db:
    image: mariadb:10.3
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: crawler
    ports:
      - 3306:3306
    tmpfs:
      - /var/lib/mysql/:rw,noexec,nosuid
